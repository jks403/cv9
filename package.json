{
  "name": "weatherproject",
  "version": "1.0.0",
  "description": "weather api to enter in data and query the parameters and return the request to the client browser",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "joshua k stewart",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.18.2"
  }
}
